# Azure AI Configuration
PROJECT_CONNECTION_STRING="uksouth.api.azureml.ms;subscription_id;resource_group;project_name"
MODEL_DEPLOYMENT_NAME="gpt-4.1"

# For CI/CD - Service Principal Authentication (optional)
# If not set, will use Azure CLI credentials (az login) for local development
# AZURE_CLIENT_ID="service-principal-app-id"
# AZURE_TENANT_ID="your-tenant-id"
# AZURE_CLIENT_SECRET="service-principal-password"

# Database Configuration (PostgreSQL from Supabase)
SQL_CS="postgresql://user:password@host:port/database"

# Server Configuration
PORT=8000

# Optional Features
ENABLE_AUTO_EVALUATION=false
AUTO_EVAL_METRICS="intent,coherence,tools"