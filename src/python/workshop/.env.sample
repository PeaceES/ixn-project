# Model deployment name in your Azure OpenAI / Azure AI project
MODEL_DEPLOYMENT_NAME="gpt-5-chat"
# Bing grounding connection
BING_CONNECTION_NAME="groundingwithbingsearch"
# Azure AI Project connection string (subscription;resourceGroup;projectName) – fill in before running
PROJECT_CONNECTION_STRING=""

# Azure OpenAI / Azure AI Inference endpoint (project or resource endpoint)
AZURE_OPENAI_ENDPOINT="https://ucabp-mckw43ar-eastus2.services.ai.azure.com/"
# API key 
AZURE_OPENAI_API_KEY=""
# API version (update if model requires a newer version)
AZURE_OPENAI_API_VERSION="2023-05-15"

# Auto-evaluation settings
ENABLE_AUTO_EVALUATION=true
AUTO_EVAL_METRICS=intent,coherence,tools

# SECURITY NOTES:
# 1. Rotate keys immediately if they were ever shared or committed.
# 2. Prefer AAD (Managed Identity / Service Principal) over API keys where possible.
# 3. Never commit a real key to version control.